api_version: aqs.campaign.v1
campaign_name: cuda_graphs_ablation_v1
objective: steady_state
system_manifest: configs/systems/ovh_gra9_rtx5000_28.yml
outdir: artifacts/campaigns/cuda_graphs_ablation_v1
workloads:
  - workloads/manifests/imported/real_ghz3_amplitude.yaml
  - workloads/manifests/imported/real_dense_ring6_batched.yaml
plan_source:
  kind: planner_templates
matrix:
  planner_budget: [balanced]
  measurement_repeats: [3]
  repeat_count_hint: [8, 32]
  autotune: [true]
  reuse_cache: [true]
  graph_mode: ["off", "warm_only", "steady_state"]
replicates: 3
execution_intent: require_real
probe_strategy: cuquantum_if_available
profile_policy:
  nsys: representative_only
  ncu: representative_only
notes: CUDA Graphs foundation ablation manifest. Measured execution is deferred to the remote CUDA host pass; unsupported capture must be reported explicitly rather than silently hidden.
